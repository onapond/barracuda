# Barracuda Codex Review: Claude Work Audit
Date: 2026-03-27
Owner: Codex Main Agent
Status: Review complete

## Scope

This document audits the Claude-attributed work currently visible in the repository and separates:

- committed source changes
- committed documentation and approval artifacts
- generated or runtime artifacts still left in the worktree
- approval status under `AGENTS.md` and `docs/barracuda_agent_team_operating_model.md`

---

## Findings

### 1. Recent 2026-03-26 changes are not fully approval-complete

Severity
- high

Evidence
- Recent committed work clearly exists in `2369797`, `d8e4fda`, `1b18aa6`, `26d3c9b`, `f71d423`, `dd13f7c`, `d991550`, `87ed5be`
- The changes are summarized in `docs/barracuda_codex_handoff_status.md`
- Matching 2026-03-26 route-cycle artifacts for Orchestrator, Responsive QA, and Frontend QA were not found under `docs/cycles/`

Judgment
- These changes are real and committed
- These changes are not final-approved under the active Barracuda operating model
- Status must remain `provisional` or `revise`, not `approved`

Impact
- Do not treat the 2026-03-26 image optimization and line-break work as fully signed off
- Any follow-up decision should require either missing artifacts or an explicit user decision to accept the risk

### 2. 2026-03-24 workflow artifacts are comparatively complete

Severity
- medium

Evidence
- Homepage copy sign-off exists in `docs/cycles/2026-03-24_orchestrator_copy_signoff.md`
- `/brand` and `/coffee` Frontend QA exists in `docs/cycles/2026-03-24_batch1_frontend_qa_brand_coffee.md`
- Batch 1 Orchestrator post-integration decision exists in `docs/cycles/2026-03-24_batch1_orchestrator_post_integration_brand_coffee.md`
- Homepage signature tabs Orchestrator post-QA decision exists in `docs/cycles/2026-03-24_signature_tabs_orchestrator_post_qa.md`

Judgment
- The 2026-03-24 work is the most trustworthy reviewed portion of the Claude workstream
- Approval is still scoped, not blanket approval for the whole site

Impact
- When reusing prior work as a baseline, prefer the 2026-03-24 approved scopes first

### 3. Current worktree contains non-source runtime artifacts and generated state

Severity
- medium

Evidence
- Modified tracked files:
  - `docs/.pdca-status.json`
  - `docs/.bkit-memory.json`
- Untracked runtime and QA artifacts include:
  - `.chrome-cdp/`
  - `.chrome-qa-runtime/`
  - `.chrome-qa-runtime-2/`
  - `.chrome-shot/`
  - `.qa-homepage/`
  - `test-results/`
  - `tmp-ref*.png`
  - `tmp-ref*.html`
  - `public/images.zip`
  - `docs/.pdca-snapshots/`
  - `.claude/settings.local.json`
  - `_newfile_test.txt`
  - `_write_test.txt`

Judgment
- These are not the same as user-facing source changes
- Most are generated by QA, browser automation, or local tool sessions

Impact
- They should not be mistaken for unresolved product implementation work
- They should be reviewed separately before any cleanup or commit decision

### 4. The tracked JSON changes are session-state updates, not product logic changes

Severity
- low

Evidence
- `docs/.pdca-status.json` only advances timestamps/history and changes `lastFile` to `components/company-signature-tabs.tsx`
- `docs/.bkit-memory.json` only updates `sessionCount` and `lastSession`

Judgment
- These are bookkeeping artifacts
- They do not change site behavior

Impact
- If the goal is product review, these files can be ignored
- If the goal is exact workspace provenance, they should be documented but not mixed with app changes

---

## Claude Work Summary

### A. 2026-03-24 copy and workflow build-out

Representative commit
- `0b3eec1` `copy: rewrite all route copy across brand, coffee, space, menu, store, visit`

Observed work
- route copy rewrite in `data/sites/barracuda.ts`
- Barracuda agent workflow and handoff docs added under `docs/`
- Playwright QA setup added:
  - `playwright.config.ts`
  - `tests/homepage-breakpoint.spec.ts`
- homepage and shared company components updated to align with approved copy

Review status
- partially approved with explicit scope artifacts present

### B. 2026-03-25 real image rollout across routes

Representative commits
- `1b958ed`
- `227dd6d`
- `4bb8c58`
- `ddbc152`
- `c99f2a2`
- `7f4e4b8`
- `5fd9d0b`
- `b97d932`

Observed work
- real image assets rolled out to landing, brand, coffee, space, store, and visit routes
- content references updated in `data/sites/barracuda.ts`
- `public/images/IMAGE_SPEC.md` updated as the image guide/source note

Review status
- committed and traceable
- not backed by a full approval chain visible in `docs/cycles/` for all affected routes

### C. 2026-03-26 image optimization and Korean line-break fixes

Representative commits
- `2369797`
- `d8e4fda`
- `1b18aa6`
- `26d3c9b`
- `f71d423`
- `dd13f7c`
- `d991550`
- `87ed5be`

Observed work
- image format conversion and compression
- `sizes`, `placeholder="blur"`, and `blurDataURL` rollout
- OG/Twitter metadata additions in `app/layout.tsx`
- Korean line-break rendering fixes in shared company components
- archive and handoff notes updated

Key files
- `app/layout.tsx`
- `next.config.ts`
- `lib/image-blur.ts`
- `scripts/optimize-images.mjs`
- `components/company-page-hero.tsx`
- `components/section-heading.tsx`
- `components/company-info-section.tsx`
- `components/company-gallery-section.tsx`
- `components/company-menu-showcase.tsx`
- `components/company-signature-tabs.tsx`

Review status
- committed
- technically coherent from commit messages and touched files
- not fully approved under the current operating rules because required downstream review artifacts are missing

---

## Approval Classification

### Approved

- 2026-03-24 homepage copy sign-off scope only
- 2026-03-24 `/brand` and `/coffee` Batch 1 approved integration scope
- 2026-03-24 homepage signature tabs rendering scope

### Provisional

- 2026-03-25 real-image rollout across multiple routes
- 2026-03-26 image optimization changes
- 2026-03-26 Korean line-break fixes

### Revise

- Any claim that the latest 2026-03-26 changes are fully approval-complete
- Any attempt to collapse committed code changes and generated QA artifacts into one status

### Blocked

- Treating missing approval gates as implicitly passed
- Treating technical completion alone as final sign-off

---

## Additional Uncommitted Changes Review

### Tracked modified files

1. `docs/.pdca-status.json`
   - session-state history extended
   - no product logic change detected
2. `docs/.bkit-memory.json`
   - memory/session metadata updated
   - no product logic change detected

### Untracked files and folders

Observed categories
- browser runtime crash/state directories
- screenshot and QA output
- local temp HTML and PNG references
- packaged image archive
- local Claude settings
- scratch test files

Assessment
- No additional uncommitted application source changes were found
- The extra worktree noise is operational, not implementation-level

---

## Recommended Next Step

If the team wants to continue from Claude's work safely:

1. Treat 2026-03-24 approved scopes as the last clean approval baseline
2. Decide whether 2026-03-25 and 2026-03-26 work should be:
   - retroactively documented and reviewed, or
   - accepted as user-risk overrides
3. Separate runtime artifacts from source review before any commit or cleanup step

---

## Reference Files

- `AGENTS.md`
- `docs/barracuda_agent_team_operating_model.md`
- `docs/barracuda_codex_handoff_status.md`
- `docs/cycles/2026-03-24_orchestrator_copy_signoff.md`
- `docs/cycles/2026-03-24_batch1_frontend_qa_brand_coffee.md`
- `docs/cycles/2026-03-24_batch1_orchestrator_post_integration_brand_coffee.md`
- `docs/cycles/2026-03-24_signature_tabs_orchestrator_post_qa.md`

---

## Detailed Technical Review

### Finding 1. Missing OG image breaks the newly added social metadata path

Severity
- high

Evidence
- `app/layout.tsx` references `/images/og-image.jpg` in Open Graph and Twitter metadata
- `public/images/og-image.jpg` does not exist in the current worktree
- `docs/barracuda_codex_handoff_status.md` already notes that the user still needs to prepare this file

Code reference
- `app/layout.tsx:24`
- `app/layout.tsx:38`

Impact
- social preview cards can render without the intended image
- build and lint do not catch this because the metadata path is valid syntactically

Codex interpretation
- treat metadata image work as incomplete until the asset exists or the metadata is changed to a real asset

### Finding 2. Active workflow documents currently contradict each other on copy approval authority

Severity
- high

Evidence
- `docs/barracuda_codex_handoff_status.md` says copy is handled directly by main Claude and does not go through the agent-team KCE gate
- `AGENTS.md` says all substantive work runs through the Orchestrator by default and missing approval gates cannot be treated as passed
- `docs/barracuda_agent_team_operating_model.md` says typography/line-break work cannot begin until Korean Copy Editor approves copy at sentence level

Code / doc reference
- `docs/barracuda_codex_handoff_status.md:11`
- `AGENTS.md:7`
- `AGENTS.md:27`
- `docs/barracuda_agent_team_operating_model.md:21`
- `docs/barracuda_agent_team_operating_model.md:29`
- `docs/barracuda_agent_team_operating_model.md:167`

Impact
- future Codex work can easily follow the wrong rule set
- the same change can be judged approved by one document and blocked by another

Codex interpretation
- use `AGENTS.md` plus the active operating model as the higher-priority source of truth
- treat the handoff-status exception as a provisional session note, not a stronger rule

### Finding 3. The stated Korean line-break root cause was only partially removed

Severity
- medium

Evidence
- `docs/barracuda_codex_handoff_status.md` describes `text-wrap: balance` as one of the root causes of awkward Korean wrapping
- global utility `.type-wrap-balance` still sets `text-wrap: balance`
- shared title components still use `.type-wrap-balance`
- homepage signature tab headings and tab button titles still use `.type-wrap-balance`

Code / doc reference
- `docs/barracuda_codex_handoff_status.md:54`
- `app/globals.css:91`
- `components/page-hero.tsx:68`
- `components/page-hero.tsx:78`
- `components/section-heading.tsx:46`
- `components/section-heading.tsx:57`
- `components/company-signature-tabs.tsx:31`
- `components/company-signature-tabs.tsx:34`
- `components/company-signature-tabs.tsx:48`

Impact
- the reported fix addresses the overlay text case, but not the broader shared typography system
- future Korean copy updates can still regress in headings that rely on shared `type-wrap-balance`

Codex interpretation
- do not assume Korean line-break policy is solved globally
- re-verify all shared heading surfaces before doing copy or layout edits

### Finding 4. Brand spelling is inconsistent across metadata, content, and docs

Severity
- medium

Evidence
- Most strategy and operating docs use `Barracuda`
- runtime metadata and route content repeatedly use `Baracuda`

Code / doc reference
- `app/layout.tsx:15`
- `data/sites/barracuda.ts:339`
- `data/sites/barracuda.ts:379`
- `data/sites/barracuda.ts:435`
- `data/sites/barracuda.ts:508`
- `data/sites/barracuda.ts:567`
- `data/sites/barracuda.ts:609`

Impact
- SEO naming, social metadata, subject lines, alt text, and brand consistency can drift
- future copy review will waste time unless one spelling is explicitly locked

Codex interpretation
- do not normalize this automatically
- first confirm whether `Baracuda` is the intentional public spelling or a typo carried across assets

### Finding 5. Build and lint currently pass, so the remaining issues are policy/content risks rather than compile failures

Severity
- low

Evidence
- `npm run build` passed on 2026-03-27 local review
- `npm run lint` passed on 2026-03-27 local review

Impact
- the project is technically runnable
- approval and content-risk review still remain necessary

Codex interpretation
- use build/lint pass as a floor, not as a release decision

---

## Codex Baseline For Next Work

Use the following baseline before making new changes:

1. Approval baseline
   - trusted approved scope: 2026-03-24 homepage copy sign-off, Batch 1 `/brand` + `/coffee`, homepage signature tabs cycle
   - unapproved latest scope: most 2026-03-25 and 2026-03-26 image and line-break work

2. Code baseline
   - current branch head is technically healthy: build and lint pass
   - missing asset and typography-policy risks still exist

3. Process baseline
   - prioritize `AGENTS.md` and `docs/barracuda_agent_team_operating_model.md`
   - do not inherit ad hoc exceptions from handoff notes unless the user explicitly confirms them

4. Workspace baseline
   - ignore runtime artifact folders during product review
   - do not confuse `.chrome-*`, `.qa-homepage/`, `test-results/`, `tmp-ref*`, and session JSON churn with actual feature work
