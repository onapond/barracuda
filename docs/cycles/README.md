# Cycles

This folder holds cycle-scoped artifacts used as evidence for Barracuda agent-team approval gates.

Naming convention
- `YYYY-MM-DD_<scope>_<artifact>.md`

Notes
- Artifacts in this folder are required for approval gates defined by the Orchestrator command packages.
- Keep artifacts factual and reviewable. Avoid prose that cannot be verified against the codebase.

